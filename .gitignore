# Local secrets — never commit these
Secrets.ini
rescuetime.ini

# Personal RescueTime data — generated by the workflow (deployed straight to
# GitHub Pages, never committed) and by local Fetch-Data.ps1 runs.
docs/data.json
docs/archive.json
